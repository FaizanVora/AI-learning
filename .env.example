# Copy to .env and fill in real values. Never commit .env.
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=
# owl-alpha may return "No endpoints found" depending on account/routing;
# openai/gpt-4o-mini is a reliable default for learning.
OPENROUTER_MODEL=openai/gpt-4o-mini
APP_ENV=development
